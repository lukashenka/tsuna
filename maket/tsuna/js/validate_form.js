
			<!--
			function validate_form ( )
			{
				valid = true;

					if ( document.authorize_form.login.value == "" )
					{
							alert ( "Пожалуйста, введите Ваш логин." );
							valid = false;
					}

					
					if ( document.authorize_form.pass.value == "" )
					{
							alert ( "Вы забыли ввести пароль" );
							valid = false;
					}
					
					
					if ( document.authorize_form.login.value > "а" )
					{
							alert ( "Логин может состоять только из латинских букв" );
							valid = false;
					}
					
					if ( document.authorize_form.login.value > "я" )
					{
							alert ( "Логин может состоять только из латинских букв" );
							valid = false;
					}
					
					if ( document.authorize_form.pass.value > "а" )
					{
							alert ( "Пароль может состоять только из латинских букв" );
							valid = false;
					}
					
					if ( document.authorize_form.pass.value > "я" )
					{
							alert ( "Пароль может состоять только из латинских букв" );
							valid = false;
					}

					return valid;
			}
			//-->
		